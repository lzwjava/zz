# Pinned to what was validated on this machine (RTX 4070 12 GB, Python 3.14, CUDA 13):
#   torch 2.10.0+cu128, transformers 5.14.1
trl>=1.13.0
peft>=0.21.0
bitsandbytes>=0.50.2
accelerate>=1.10.0
datasets>=4.0.0
# optional
# wandb
# flash-attn   # only if you want --attn flash_attention_2 (slow to build; sdpa is fine)
